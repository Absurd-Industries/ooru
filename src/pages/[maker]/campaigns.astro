---
// A maker's campaigns now live on the global /campaigns listing, filtered by
// ?maker=. This route 301-redirects there so old links keep working.
import { makers } from "../../data/makers";

export function getStaticPaths() {
  return makers.map((m) => ({ params: { maker: m.slug } }));
}

return Astro.redirect(`/campaigns?maker=${Astro.params.maker}`, 301);
---
