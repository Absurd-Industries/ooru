---
/**
 * Campaigns listing page.
 * Displays a searchable, filterable grid of open-source hardware campaigns.
 * Filtering is handled client-side by the CampaignsListing Vue island.
 */

import BaseLayout from "../../layouts/BaseLayout.astro";
import { t } from "../../i18n";
import CampaignsListing from "../../components/CampaignsListing.vue";
import { campaigns } from "../../data/campaigns";
---

<BaseLayout
  title="Campaigns"
  description="Back open-source, repairable indie hardware. Every project ships with full source files."
  ogImage="/og/campaigns.png"
  activeTab="campaigns"
>
  <section class="pt-6 pb-6 sm:pt-8 sm:pb-8">
    <div class="max-w-6xl mx-auto px-5 sm:px-6">
      <h1 class="font-serif font-bold text-3xl sm:text-4xl text-ink mb-2">
        <i class="ph-duotone ph-rocket-launch text-ink mr-2" style="font-size:1.4rem;"></i><span data-i18n="section.campaigns">{t("section.campaigns")}</span>
      </h1>
      <p class="text-ink text-base sm:text-lg max-w-xl leading-relaxed" data-i18n="page.campaigns.desc">
        {t("page.campaigns.desc")}
      </p>
    </div>
  </section>

  <section class="pb-16 sm:pb-20">
    <div class="max-w-6xl mx-auto px-5 sm:px-6">
      <!-- Interactive listing island -->
      <CampaignsListing campaigns={campaigns} client:load />
    </div>
  </section>
</BaseLayout>
